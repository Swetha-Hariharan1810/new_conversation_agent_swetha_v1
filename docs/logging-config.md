# src/agent/logging-config.yaml

Purpose
- YAML configuration for Python logging: formatters, handlers, and logger levels used by the agent.

Notes
- Used by logger.py to configure the logging subsystem.
- Review handler names and levels to understand where logs are emitted (console/file/etc.).

Source
- https://github.com/Swetha-Hariharan1810/new_conversation_agent_swetha_v1/blob/main/src/agent/logging-config.yaml
