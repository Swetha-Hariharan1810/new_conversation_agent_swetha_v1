services:
  cloud-security-scanner:
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    env_file:
      - .env
    image: tenable/cloud-security-scanner:latest
    command: container-image scan --name conversation-agent:latest --fail-on-min-severity high
